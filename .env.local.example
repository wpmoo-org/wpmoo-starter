# Local WordPress environment for pre-commit tests (MAMP/XAMPP/etc.)
# Copy this file to .env.local and adjust values.

# Absolute path to your local WordPress installation directory.
# Example (MAMP): /Applications/MAMP/htdocs/wordpress
WP_PATH=/absolute/path/to/your/wp

# Site URL used by WP-CLI (optional but recommended)
# Example: http://localhost:8888/wordpress
WP_URL=http://localhost

# Path to WP-CLI binary (defaults to "wp" in PATH)
WP_CLI_BIN=wp

# Plugin slug (defaults to wpmoo-starter)
PLUGIN_SLUG=wpmoo-starter

# Make local plugin check strict (fail commit on errors)
#LOCAL_STRICT=1

